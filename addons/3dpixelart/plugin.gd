@tool
extends EditorPlugin
## 3D Pixel Art — editor plugin.
##
## All runtime types are registered via class_name, so this plugin only
## needs to exist for the addon to be self-contained. Requirements:
## Forward+ renderer (CompositorEffects are used for the screen passes).


func _enter_tree() -> void:
	pass


func _exit_tree() -> void:
	pass
